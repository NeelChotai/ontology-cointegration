def main():
    # after reading in ontology data, create a sorted list descending by highest cointegration factor
    # performance can be returned individually if user requests or generate graphs
    print("Hello world.")

def generate():
    # generate and sort SPARQL queries here
    # return as CSV?
    # return format: {} where key = ticker pair and 
    print("todo")

def evaluate(ticker, days):
    # read in ticker, output percentage increase within x days (optional)
    # if days not specified, presume entire timeline
    # return percentage increase
    print(todo)